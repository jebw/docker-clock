machine:
  services:
    - docker

dependencies:
  override:
    - docker -v
    - docker info
    - bundle install
    - rake docker:build
    - docker run -p 4567:4567 -d jebw/clock:latest
    - which curl
    - 'curl -v http://127.0.0.1:4567/'